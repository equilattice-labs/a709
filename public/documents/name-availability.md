# Temvorel — selected identity

**Temvorel / temvorel.xyz / @temvorel** is the final prepared identity. The name combines tempo, vow and release and is pronounced **TEM-vor-el**: commitments released with a clear rhythm. The tagline is **Value, on your terms.** This is a product identity; no new token or ticker is introduced.

On **17 September 2026, 14:47:11–13 UTC**, authoritative registry WHOIS checks returned no registered object for `temvorel.xyz` and `temvorel.top`, and explicitly reported `temvorel.fun` as available for registration. The matching registry RDAP checks returned structured 404 responses. At **14:45:45 UTC**, X's official username endpoint returned `valid: true`, `reason: available`, `msg: Available!` for `temvorel`; negative controls returned unavailable results.

The available local brand records contain no prior Temvorel match. Quoted and unquoted rendered Bing searches showed no same-spelling result title or snippet. GitHub repository and user searches, the npm registry and CoinGecko found no match. Coverage is limited to inspected sources and returned results; several other search engines required human verification. **This does not prove that the word has never existed anywhere online or establish trademark clearance.**

No identifier was bought, registered, reserved or published. Availability can change immediately, and final eligibility and pricing depend on the registration flow.

The full dated methods, responses, controls and limitations are in [the identity evidence record](rebrand-2026/name-availability.md). Earlier identities and their dated checks are retained only as history in `history/pre-rebrand-2026-09-17/`.
